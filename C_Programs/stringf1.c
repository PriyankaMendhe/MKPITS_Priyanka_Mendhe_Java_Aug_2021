//12 wap to find length of a string using string function
#include<stdio.h>
#include<string.h>
int main()
{
	int length;
	char string[]="Priyanka Mendhe";
	length=strlen(string);//counted including space
	printf("%d is a length of string",length);
}
