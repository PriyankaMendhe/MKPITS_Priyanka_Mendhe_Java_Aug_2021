//5- wap to accept and display name using gets and puts function.
//gets(); used for scanf with space 
//puts(); for printf
#include<stdio.h>
int main()
{
	char full_name[20];
	puts("Enter string:");
	gets(full_name);
	puts(full_name);
}
